---
title: C6239
ms.date: 11/04/2016
ms.topic: reference
f1_keywords:
- C6239
helpviewer_keywords:
- C6239
ms.assetid: c80e02bc-ff54-4fde-8c1c-5852853bed24
author: mikeblome
ms.author: mblome
manager: markl
ms.workload:
- multiple
ms.openlocfilehash: 5270a1c1092e14216473db5352785a64eee8ceed
ms.sourcegitcommit: 535ef05b1e553f0fc66082cd2e0998817eb2a56a
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 10/07/2019
ms.locfileid: "72013526"
---
# <a name="c6239"></a>C6239
aviso C6239: (\<non-zero constante > & & \<expression >) sempre é avaliada como o resultado da @no__t >-2expression. Você pretendia usar o operador and de bit a passo?

 Esse aviso indica que um valor constante diferente de zero, diferente de um, foi detectado no lado esquerdo de uma operação AND lógica que ocorre em um contexto de teste. Por exemplo, a expressão `( 2 && n )` é reduzida para `(!!n)`, que é o valor booliano de `n`.

 Esse aviso normalmente indica uma tentativa de verificar uma máscara de bits na qual o operador bit-a-a (`&`) deve ser usado e não será gerado se a constante diferente de zero for avaliada como 1 devido ao seu uso para escolher seletivamente os caminhos de código.

## <a name="example"></a>Exemplo
 O código a seguir gera este aviso:

```
#include <stdio.h>
#define INPUT_TYPE 2
void f( int n )
{
   if(INPUT_TYPE && n) // warning 6239
   {
      puts("boolean value of n is true");
   }
   else
   {
      puts("boolean value of n is false");
   }
}
```

 Para corrigir esse aviso, use o operador bit e (`&`), conforme mostrado no código a seguir:

```
#include <stdio.h>
#define INPUT_TYPE 2
void f( int n )
{
   if( ( INPUT_TYPE & n ) )
   {
      puts("bitmask true");
   }
   else
   {
      puts("bitmask false");
   }
}
```

## <a name="see-also"></a>Consulte também
 [Operador &](/dotnet/csharp/language-reference/operators/and-operator)